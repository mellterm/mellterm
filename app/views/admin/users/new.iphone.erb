<h3><%= @title = "Create a new User" %></h3>

<% form_for(@user, :url => admin_users_path, :html => { :multipart => true }) do |f| %>

  <%= f.error_messages %>
  
  <%= render :partial => 'form', :locals => { :f => f, :user => @user } %>

  <%= image_submit_tag "admin/save_button.png", :class => "button" %>

<% end %>


