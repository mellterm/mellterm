<% form_for(@document, :html => { :multipart => true }) do |f| %>
  
  <%= f.error_messages %>
  
  <%= render :partial => 'form', :locals => { :f => f, :document => @document } %>
  
  <p>
    <%= f.submit 'Update' %>
  </p>
<% end %>

<%= link_to 'Show', @document %> |
<%= link_to 'Back', documents_path %> | 
<%= link_to 'New', new_document_path %>