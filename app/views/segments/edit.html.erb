<% form_for @segment, :url => { :action => "update" } do |f| %>

  <%= f.error_messages %>
  
  <%= render :partial => 'form', :locals => { :f => f, :segment => @segment } %>

  <%= image_submit_tag "admin/save_button.png", :class => "button" %>
  
<% end %>
