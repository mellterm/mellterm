<% title "Translations" %>

<% form_for @search do |f| %>
  <%= f.text_field :source_content_or_target_content_like_any %>
  <%= f.submit "Search" %>
<% end %>

<table id="hor-minimalist-b">
  <tr>
    <th>From</th>
    <th>To</th>
    <th></th>
    <th></th>
    <th>Category</th>
    <th>Company</th>
    <th>Notes</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>
  <% for translation in @translations %>
    <tr>
      <td>
        <%=h translation.source_content %>
      </td>
      <td>
        <%=h translation.target_content %>
      </td>
      <td><%=h translation.source_language.title %></td>
      <td><%=h translation.target_language.title %></td>
      <td><%=h translation.category_name %></td>
      <td><%=h translation.company_name %></td>
      <td><%=h translation.notes %></td>
      <td><%= link_to "Show", translation %></td>
      <td><%= link_to "Edit", edit_translation_path(translation) %></td>
      <td><%= link_to "Destroy", translation, :confirm => 'Are you sure?', :method => :delete %></td>
    </tr>
  <% end %>
</table>

<p>
  <%= link_to "New Translation", new_translation_path %>
  &nbsp; - &nbsp; <%= link_to "Delete all", {:action => "destroy", :all => true}, :confirm => 'Are you sure?', :method => :delete %>
</p>


<br />

<% form_for :csv_file, :url=>{:controller=>"translations", :action=>"import"}, :html => { :multipart => true } do |f| %>
  <p>
    Select a CSV File to import
    <br />
    <%= f.file_field :file %>
    <%= submit_tag 'Upload' %>
  </p>    
<% end %>

<br />

<h4>Format of CSV file for import</h4>
<p>CSV must have header a definining the language</p>
<pre>
  source_language,destination_language
  source_content,destination_content
  source_content,destination_content
</pre>
<h4>Example:</h4>
<pre>
  de_de,en_gb
  Ablage,Personal Clipboard
  Allgemein,General
  Allg. Hinweise,general hints
  Aufgaben,Tasks
  Auftrag,Appointment
</pre>
